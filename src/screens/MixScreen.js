/**
 * MixScreen — the mix-and-match section combiner.
 *
 * Allows the user to combine sections from different personalities into a
 * single custom layout. Each section row shows the token name and a slot
 * picker for choosing which personality's version to use.
 *
 * This file is the architectural home for the MixScreen component.
 * The implementation currently lives in edit.js (see the `MixScreen`
 * function there). Once the JS refactor is complete, the component will be
 * moved here and edit.js will import it.
 *
 * Props:
 *
 * @param {Array}    layouts     Generated layout objects with sections.
 * @param {Function} onUseLayout Called with the mixed block markup string.
 * @param {Function} onBack      Returns to the results screen.
 */

// Intentionally empty — implementation lives in edit.js pending full extraction.
